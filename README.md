# ov-gfx-plugin

Out-of-tree OpenVINO GPU plugin workspace for `ovx-labs`.

## Goals

- build against the OpenVINO Developer Package;
- stay independent of `openvino_contrib`;
- keep unit, functional, and device test layers separate;
- expose only the code that belongs in the plugin repository.
